<tr class="<%= cycle('odd', 'even') %>">
  <td><%=h user.login %></td>
  <td><%=h user.email %></td>
  <td><%= user.enabled ? 'yes' : 'no' %>
  <% unless user == current_user %>
	  <% if user.enabled %>
			<%= link_to('desativado', user_path(user.id), :method => :delete) %>
		<% else %>
			<%= link_to('ativado', enable_user_path(user.id), :method => :put) %>
		<% end %>
  <% end %>
  </td>
  <td><%= link_to 'editar permissões', user_roles_path(user) %></td>
</tr>

